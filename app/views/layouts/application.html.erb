<!DOCTYPE html>
<html>
  <head>
    <title>JobsManagement</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <p><strong><%= link_to 'JOB HOME', root_path %></strong>
    <% if user_signed_in? %>
      <%= current_user.email %>
      <%= link_to 'Log out', destroy_user_session_path, method: :delete %>
    <% elsif %>
      <% candidate_signed_in? %>
      <%= link_to 'Editar Perfil', edit_candidate_registration_path %>
      <%= link_to 'Log out', destroy_candidate_session_path, method: :delete %>
    <% else %>
      <%= link_to 'Log in CANDIDATO', new_candidate_session_path %>
    <% end %>
    </p>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
